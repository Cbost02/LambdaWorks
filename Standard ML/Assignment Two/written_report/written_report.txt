I. Reference ...

    i.) Not much references outside of the class needed to be made in order to complete the assignment. 

II. Design ...

    The design for each of the constructed recursion I have on file follow the logic gone over in class. Most follow the format of Level 2,3,4, & 5 recursions. Some of these ML Program were created with functions that used a ton of parameters to complete the task.

III. Difficulty ...

    This assignment, though harder than the first, put the skills I've gained from the class to the test. I'll say that I wished I gained a bit more than what I'd hoped as I couldn't complete problems 8 & 9.

IV. Comments ...

    I'd like to practice more stuff like this in other languages. I've grown to adore the functional programming paradigm. Haskell & ML are some of my go-to languages.

Appendix One -- Source Code

    The following code is a demo of programs 6 & 7.

#6
    (* FUNCTION NAME: smap() *)
(* DESCRIPTION: This functions 'maps' or changes each element of a list by applying a lambda statement.*)
fun smap(F,nil) = nil
|   smap(F,x::xs) = F x :: smap(F,xs);


(* FUNCTION NAME: helper() *)
(* DESCRIPTION: This function serves as the helper function to chcase()!*)
fun helper(L) = smap(fn x => if ord(x) >= 97 then chr(ord(x) - 32) else x, L);


(* FUNCTION NAME: chcase() *)
(* DESCRIPTION: This function searches for lowercase char within a string 'L' and capitalizes them!*)
fun chcase(L) = implode(helper(explode L));



#7

    (* FUNCTION NAME: smap() *)
(* DESCRIPTION: This functions 'maps' or changes each element of a list by applying a lambda statement.*)

fun smap(_, nil) = nil
|   smap(F,x::xs) = F x :: smap(F,xs);


(* FUNCTION NAME: infront1() *)
(* DESCRIPTION: Inserts a specified value 'a' at the front of each list.*)
fun infront1(a,L) = smap(fn x => a::x, L);



Appendix Two -- Sample Runs 

    Inputs

infront1(1,[[1,2],nil,[3]]);
infront1(1.0,[[5.6,6.7],[1.2,7.8,5.6],nil]);
chcase("Cromwell");
chcase("Emerald");

    Outputs




val it = [[1,1,2],[1],[1,3]] : int list list
val it = [[1.0,5.6,6.7],[1.0,1.2,7.8,5.6],[1.0]] : real list list
val it = "CROMWELL" : string
val it = "EMERALD" : string
